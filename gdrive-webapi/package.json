{
  "name": "@LuannMateus/gdrive-webapi",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "engines": {
    "node": "16"
  },
  "scripts": {
    "start": "mkdir -p downloads && node src/index.js",
    "prod": "mkdir -p downloads && NODE_ENV=production node src/index.js",
    "test": "NODE_OPTIONS=--experimental-vm-modules npx jest --runInBand",
    "test:watch": "npm test -- --watchAll",
    "test:cov": "npm test -- --no-cache --coverage"
  },
  "keywords": [],
  "author": "LuannMateus",
  "license": "ISC",
  "devDependencies": {
    "jest": "^27.1.0"
  },
  "dependencies": {
    "busboy": "^0.3.1",
    "form-data": "4.0",
    "pino": "^6.8.0",
    "pino-pretty": "^5.1.3",
    "pretty-bytes": "^5.6.0",
    "socket.io": "^4.1.3"
  }
}
